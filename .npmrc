# This project uses Yarn Berry (Yarn 4+) instead of npm
# Please use 'yarn' commands instead of 'npm'
# 
# Examples:
#   yarn install (instead of npm install)
#   yarn add <package> (instead of npm install <package>)
#   yarn dev (instead of npm run dev)

engine-strict=true 